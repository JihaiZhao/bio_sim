# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
env/

# uv
.uv-cache/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# Isaac Sim / Isaac Lab artifacts
logs/
outputs/
runs/
wandb/
_isaac_sim/
*.usd.bak
NvStreamer-*.etli

# Build
dist/
build/

# Vendored third-party clones (curobo etc.) — not tracked here
third_party/

# Assets — hosted on HuggingFace, fetched via scripts/fetch_assets.py
# (.gitignore here only stops NEW additions from sneaking in. The handful
# of files already tracked under assets/ are kept until the HF migration
# is finalised; remove them then with `git rm --cached -r assets/`.)
assets/
